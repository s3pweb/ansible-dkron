---
# Create some jobs and then return a list of jobs in the cluster

- hosts: localhost
  gather_facts: False
  collections:
    - knightsg.dkron
  vars:
    query_host: "{{ groups['dkron']|map('extract', hostvars, ['ansible_host'])|first }}"
    
  tasks:

    - name: test dkron_job_info module
      dkron_job_info:
        endpoint: "{{ query_host }}"
        job_name: job2
        limit_history: 5
      register: data

    - name: display cluster job info
      debug:
        var: data